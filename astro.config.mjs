// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config configuracion para el despliegue en github pages
export default defineConfig({
	site: 'https://cesararathap.github.io',
	base: '/portafolio-2',
});
